const fs = require('fs'); const files = ['src/data/klenco-floor-cleaning.ts', 'src/data/klenco-high-pressure.ts', 'src/data/klenco-vacuum-cleaners.ts', 'src/data/klenco-chemicals.ts']; const regex = /"(\/content_images\/[^"]+)"/g; let missing = []; files.forEach(f => { let c = fs.readFileSync(f, 'utf8'); let m; while((m = regex.exec(c)) !== null) { if(!fs.existsSync('public' + m[1])) missing.push(m[1]); } }); console.log('Missing Klenco:', missing);
